<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/ActionBarColor</item>
    </style>
    <style name="ActionBarColor" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/themeColor</item>
        <item name="background">@color/themeColor</item>
    </style>

</resources>
